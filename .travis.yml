language: java

jdk:
  - openjdk8
  - openjdk11

script:
  - cd alma-lib
  - gradle -i build test install
  - echo "Installed alma-lib"
  - ls -alh ~/.m2/repository/org/opencadc/
  - cd ..
  - echo "Now in $(pwd)"
  - ./gradlew -i assemble check javadoc
